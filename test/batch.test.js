var assert = require('chai').assert;

suite('batch', function() {
  setup(function() {
  });

  test('test', function() {
    var actual = 'foo';
    var expected = 'foo';
    assert.equal(actual, expected);
  });
});
